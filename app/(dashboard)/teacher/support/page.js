"use client";

// Reuse the existing Tutor/Counselor support experience for Teacher
// by importing the shared SupportChat widget and adapting copy only.
import TutorSupportPage from "../../tutor/support/page";

export default function TeacherSupportPage() {
  // Simply render the tutor support page which already matches the UX used elsewhere
  return <TutorSupportPage />;
}


